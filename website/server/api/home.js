// import { homeAPI } from "~/constants/api";
// import { ifetch } from '~/composables/ifetch';

export default defineEventHandler(async (event) => {
//   const data = await ifetch(homeAPI.getBanners);
//   return data;
return "data";
});